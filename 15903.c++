#include <iostream>
#include <algorithm>

using namespace std;

int n, m;
int main(void)
{
    vector<int> array;
    cin >> n >> m;

    for (int i = 0; i < n; i++)
    {
        int a;
        cin >> a;
        array.push_back(a);
    }
}